import { ThemeConfig, theme } from "antd";

export const lightTheme: ThemeConfig = {
  token: {
    colorTextLabel: "#323233",
    colorTextSecondary: "#89898a",
  },
  algorithm: theme.defaultAlgorithm,
};