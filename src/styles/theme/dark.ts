import { ThemeConfig, theme } from "antd";

export const darkTheme: ThemeConfig = {
  token: {
    colorTextLabel: "#FFFFFF",
    colorTextSecondary: "#676768",
  },
  algorithm: theme.darkAlgorithm,
};